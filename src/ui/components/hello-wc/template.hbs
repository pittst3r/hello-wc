<hello-wc>
  <form onsubmit={{action doSomething}}>
    {{#each labels key="@index" as |label|}}
      <cool-input-wc type="text">
        {{label}}
      </cool-input-wc>
    {{/each}}
  </form>
</hello-wc>
